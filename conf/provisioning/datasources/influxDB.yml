apiVersion: 1
datasources:
  - name: aura-influxDB
    orgId: 1
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    database: ${INFLUXDB_DATABASE}
    user: ${INFLUXDB_USERNAME}
    password: ${INFLUXDB_PASSWORD}
    editable: true